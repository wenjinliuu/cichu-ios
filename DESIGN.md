# 此处 · 橙色原生极简界面

用户明确方向：精致、简约、流畅；不喜欢墨绿；以仓库现有橙色图标为品牌依据。
产品事实仍以 docs/PRODUCT_PLAN.md 为准。本次不增加玻璃装饰、字体依赖、第三方动画库或持续背景动画。

## 品牌与色彩

唯一图标来源：DesignAssets/AppIcon/location-mark.svg，保留原文件不变。
品牌渐变端点：#FF8C42 → #F4511E。只用于 BrandMark 装饰，不直接拿高亮橙做小号文字。

| Token | 浅色 | 深色 |
| --- | --- | --- |
| Canvas | #FAF8F5 | #191715 |
| Surface | #FFFFFF | #25211E |
| Ink | #29231E | #F4EFE9 |
| Quiet | #71665C | #BEB1A5 |
| AccentColor | #AC3E12 | #FFAD79 |
| OnAccent | #FFFFFF | #28190F |
| Highlight | #FFF0E3 | #36261B |

操作统一橙色。地点分类仅在图标和统计编码中保留低饱和辅助色；类别名称、图标和数值共同表达信息，不只靠颜色。
PrimaryButtonStyle 显式使用 OnAccent，避免深色模式浅橙底上默认白字的潜在对比问题。

## 页面精修

| Before | After | Why |
| --- | --- | --- |
| 墨绿 hero + 装饰圆环 | 一张浅橙／深暖色当前状态卡 | 保留“在哪里、多久”这一视觉重点 |
| 大英文眉题、口号和日期重复 | 原生导航标题 + 日期 | 减少首屏占用，保留返回与标题折叠行为 |
| 全页重复圆角卡片 | 开放式分区、32pt 段间距、轻分隔 | 用排版建立层次 |
| 多彩地点卡片网格 | 图标、名称、时长组成的清晰行 | 小屏和长名称更稳定 |
| 首页完整历史不断增长 | 最近三条 + “全部”时间线 | 保留功能，减少首屏认知负担 |
| 多个分享／补记／添加按钮 | 补记在记录区；分享与添加收进右上角菜单 | 常用操作可见，其余按需展开 |
| 地图大标题浮层、大地点卡片 | 原生紧凑标题、地点标签、按需范围圈 | 让地图占主要空间 |
| 地图标注点开全屏详情 | medium / large 系统抽屉 | 保留地图位置上下文 |
| 回顾先列指标 | 先说明当前范围主要停留地点，再给图表 | 先读懂，再深入；不生成无数据支撑的比较 |
| 长期可见解释段落 | “如何计算”与“记录与权限说明”展开项 | 解释仍可找到，日常界面安静 |
| 多个互斥 boolean 弹层 | 首页／地图 enum + sheet(item:) | 保证一个页面只呈现一个主弹层 |

地图上的 material 只用于地图浮动控件及标签栏的可读性，不作为内容卡片装饰。所有页面保留系统 TabView、NavigationStack、Form、Picker、分享和编辑流程。

## 动效

- 按压：120ms ease-out，缩放 0.985；不影响布局位置。
- 当前状态／时间数字：200ms ease-out，数字更新仅在对应视图内发生。
- 地点聚焦／解释展开：280ms spring，bounce 0.08；可被下一次操作打断。
- 原生 sheet 与导航使用系统交互，不复制自定义拖拽手势。
- 回顾图表仅对范围变更做局部短动画，图表拖动读数不驱动全页动画。
- Reduce Motion 下取消位移、缩放、数字滚动和地图镜头动画；按压仍有透明度反馈。
- 不添加循环脉冲、自动入场编排、模糊文字或每行延迟出现。

## 参考技能与选择

- Impeccable：读取主 skill、craft-floor、distill；采用信息减法、渐进展示、减少卡片、明确字体层级。其 CLI 上下文加载器未运行，直接读取现有源码与产品规划。原生 iOS 使用系统字体与原生控件，按用户约束不执行编译／运行式检查。
- Emil Kowalski：读取 emil-design-eng；采用按压反馈、短促可中断动画、按目的决定动画、减少动态效果。
- Dimillian：读取 swiftui-ui-patterns 和 sheets；采用本地状态、拆分视图、互斥弹层枚举和原生抽屉。

Sources:
https://github.com/pbakaus/impeccable
https://github.com/emilkowalski/skills
https://github.com/Dimillian/Skills

## 验证边界

本次仅源码实现与静态检查，无 Xcode 编译、运行截图或动画实机验收。
已提供 DesignPreviews.swift：今日浅／深色、大字体、地图、回顾、设置的独立内存预览。预览不启用定位服务；MapKit 底图仍依赖系统地图网络。
这些是待运行的预览入口，不是已经验证通过的截图。
后续优先检查 375pt 小屏、横屏、最大字体、地图抽屉键盘、VoiceOver、Reduce Motion 和 iOS 实际深浅色渲染。


## 第五轮：原生界面审计与修正

继续采用 Impeccable 原生 audit / iOS 检查清单，并以 harden 的长文本、错误恢复和空状态原则处理问题；Emil 的局部反馈与减少无目的动效原则，以及 SwiftUI UI patterns 的原生导航和局部状态规则延续使用。未安装运行依赖。外部 skill 的运行验收要求由用户“先不编译”指令覆盖；先记录源码证据，再修正已授权的问题。

| Before | After | Why |
| --- | --- | --- |
| 补记只在更多菜单 | 最近记录区和完整时间线直接可补记 | 不需要发现隐藏入口，也不依赖图表手势 |
| 时间轴及趋势选中位置无指示 | 虚线选择线、明确读数、趋势单位“小时” | 看得清正在读哪段数据；选中不驱动全页动画 |
| 移动条使用 35% 透明度 | 使用完整 Quiet 语义色 | 加强有效数据与背景的区分 |
| 只有移动记录时趋势消失 | 任何已有记录都展示趋势 | 不让有效数据看起来像空状态 |
| 日期切换保留旧选中读数 | 切换日期或范围时清空选择 | 避免显示上一范围的选择 |
| 大字体继续挤在横排 | 共用 AdaptiveRow；大字体时纵排 | 用空间承载文字，不缩小字号 |
| 大字体下仍用圆环和分段控件 | 保留完整明细；范围用系统菜单 | 保持同一份信息可读，减少拥挤 |
| 分享失败后仍有加载转圈 | 错误说明、重新生成、文字替代 | 失败可恢复，有可执行下一步 |
| 分享主按钮位于长图下方 | 底部安全区固定分享操作 | 查看海报时仍能找到主操作 |
| 地图地点名无限延伸 | 标签两行、保留完整读屏名称；大字体详情使用 large 抽屉 | 长名称不占满横向地点导航 |
| 完整时间线空白无解释 | 显示无记录原因与补记入口 | 空状态能够引导下一步 |

新增独立预览夹具：空数据、长地点名称、最大辅助字体、仅移动记录。未运行这些预览；不以源代码替代视觉验收。具体问题、优先级与待验收项见 docs/UI_AUDIT.md。
